@* @model IEnumerable<Restoran.Models.Order>
@{
    ViewData["Title"] = "My Orders";
}
<h2>My Orders</h2>

@if (!Model.Any())
{
    <div class="alert alert-info">You have not placed any orders yet. <a asp-action="Create" asp-controller="User">Shop Now</a></div>
}
else
{
    foreach (var order in Model)
    {
        <div class="card mb-3">
            <div class="card-header">
                <strong>Order #@order.OrderId</strong>
                <span class="ml-3">Date: @order.OrderDate.ToString("dd/MM/yyyy HH:mm")</span>
                <span class="float-right">Total: <strong>@order.TotalAmount.ToString("0.00")</strong></span>
            </div>
            <div class="card-body p-0">
                <table class="table mb-0">
                    <thead>
                        <tr>
                            <th>Product</th>
                            <th style="width:120px">Quantity</th>
                            <th style="width:140px">Price</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in order.OrderItems)
                        {
                            <tr>
                                <td>@item.Product?.Name</td>
                                <td>@item.Quantity</td>
                                <td>@item.Price.ToString("0.00")</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
} *@
