@* @model Restoran.Models.OrderViewModel
@{
    ViewData["Title"] = "Products";
}

<h2>Products</h2>

<div class="row">
    @foreach (var item in Model.Products)
    {
        <div class="col-md-3 mb-3">
            <div class="card h-100">
                <img src="@item.ImageUrl" class="card-img-top" style="height:150px; object-fit:cover;" />
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title">@item.Name</h5>
                    <p class="card-text text-truncate">@item.Description</p>
                    <p class="mt-auto"><strong>Price:</strong> @item.Price.ToString("0.00")</p>

                    <form asp-action="AddItem" method="post" class="mt-2">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="prodId" value="@item.ProductId" />
                        <div class="input-group">
                            <input type="number" name="prodQty" value="1" min="1" class="form-control" style="max-width:90px;" />
                            <div class="input-group-append">
                                <button type="submit" class="btn btn-primary">Add to Cart</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    }
</div>

<div class="mt-3">
    <a asp-action="Cart" class="btn btn-success">View Cart (@(Model.OrderItems?.Count ?? 0))</a>
</div> *@
